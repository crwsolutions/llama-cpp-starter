<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LlamaCppStarterApp.ViewModels"
             x:DataType="vm:MainViewModel"
             x:Class="LlamaCppStarterApp.Views.MainPage"
             >

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Label
                Text="{Binding StatusText}"
                FontAttributes="Bold"
                SemanticProperties.HeadingLevel="Level2" />

            <Button
                Text="Run hello.exe"
                SemanticProperties.Hint="Runs E:\temp\hello.exe and streams its output"
                Command="{Binding RunHelloCommand}"
                IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                HorizontalOptions="Fill" />

            <Border
                BackgroundColor="#1E1E1E"
                StrokeShape="RoundRectangle 8"
                Padding="12"
                HorizontalOptions="Fill">
                <ScrollView HeightRequest="160">
                    <Label
                        Text="{Binding ProcessOutput}"
                        FontFamily="Consolas"
                        TextColor="White"
                        LineBreakMode="CharacterWrap" />
                </ScrollView>
            </Border>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
